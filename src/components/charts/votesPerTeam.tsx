import { ComponentType } from "react";

interface VotesPerTeamProps {
  sentiments: { sentimentAvg: number; date: Date }[];
}

const VotesPerTeam: ComponentType<VotesPerTeamProps> = ({}) => {
  return <></>;
};

export default VotesPerTeam;
